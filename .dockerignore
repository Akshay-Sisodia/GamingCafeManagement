node_modules
**/node_modules
.turbo
**/.turbo
dist
**/dist

# Desktop / .NET artifacts (not needed in the backend image)
desktop
**/bin
**/obj

# Local data & secrets
infra/data
.env
.env.*
!.env.example

# VCS / CI
.git
.github

# Docs are not needed at runtime (kept out to slim the context)
docs
