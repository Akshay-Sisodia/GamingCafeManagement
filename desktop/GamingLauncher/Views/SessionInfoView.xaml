<UserControl x:Class="GamingLauncher.Views.SessionInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="560">
        <Border Style="{StaticResource PanelCard}" Padding="40,36" MinWidth="420">
                <StackPanel>
                    <TextBlock Text="SESSION PASS" FontFamily="{StaticResource Font.Display}"
                               Foreground="{StaticResource Brush.Text}" FontSize="28"
                               FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="Your play time at this station."
                               Foreground="{StaticResource Brush.TextMuted}" FontSize="15"
                               Margin="0,0,0,28" />

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Station"
                               Foreground="{StaticResource Brush.TextFaint}" FontSize="13" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PcName}"
                               Foreground="{StaticResource Brush.Text}" FontSize="17" Margin="0,0,0,14" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Started"
                               Foreground="{StaticResource Brush.TextFaint}" FontSize="13" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding StartedAt}"
                               Foreground="{StaticResource Brush.Text}" FontSize="17" Margin="0,0,0,14" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Ends"
                               Foreground="{StaticResource Brush.TextFaint}" FontSize="13" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ExpiresAt}"
                               Foreground="{StaticResource Brush.Text}" FontSize="17" />
                </Grid>

                <Button Content="Back to games"
                        Command="{Binding DataContext.BackToGamesCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Left" Margin="0,28,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
